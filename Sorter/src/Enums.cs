namespace Sorter
{
    /// <summary>
    /// Enum for working with data types.
    /// </summary>
    public enum DataType
    {
        StringEnglish,
        StringUkrainian,
        NumberBinary,
        NumberDecimal,
        NumberHexadecimal,
        Length
    }

    /// <summary>
    /// Enum for working with data types.
    /// </summary>
    public enum SortingMethods
    {
        Bubble,
        Cocktail,
        Insertion,
        Merge,
        Selection
    }
}